vulnforge 已部署到命名空间 {{ .Release.Namespace }}。

1. 查看 API 状态：
   kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "vulnforge.name" . }}

2. 访问控制台（端口转发）：
   kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "vulnforge.fullname" . }}-web 3000:80
   然后打开 http://localhost:3000

{{- if .Values.ingress.enabled }}
3. 已启用 Ingress：
   {{- range .Values.ingress.hosts }}
   http://{{ .host }}
   {{- end }}
{{- end }}

配置通过 ConfigMap `{{ include "vulnforge.fullname" . }}-config` 注入。
默认以 mock 模式运行（无需网络/API key）。如需接入真实 LLM provider，
请编辑 values.yaml 中 config.llm 段落并 `helm upgrade`。
